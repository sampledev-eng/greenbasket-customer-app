name: greenbasket
description: A simplified BigBasket clone in Flutter.
publish_to: 'none'

environment:
  sdk: '>=2.17.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  provider: ^6.0.0
  http: ^0.13.6
  google_fonts: ^4.0.3
  shared_preferences: ^2.2.2
  connectivity_plus: ^5.0.2
  shimmer: ^3.0.0


dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
